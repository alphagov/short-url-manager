development:
  host: <%= ENV.fetch("REDIS_HOST", "127.0.0.1") %>
  port: <%= ENV.fetch("REDIS_PORT", "6379") %>
  namespace: short-url-manager
test:
  host: <%= ENV.fetch("REDIS_HOST", "127.0.0.1") %>
  port: <%= ENV.fetch("REDIS_PORT", "6379") %>
  namespace: short-url-manager
production:
  host: <%= ENV["REDIS_HOST"] %>
  port: <%= ENV["REDIS_PORT"] %>
  namespace: short-url-manager

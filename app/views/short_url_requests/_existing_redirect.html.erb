<%= render "govuk_publishing_components/components/warning_text", {
  text: "#{existing_redirect.from_path} already redirects to #{existing_redirect.to_path}! Accepting this request will overwrite
  that."
} %>
